These directories are from V1 and contain LoRA fine-tuning infrastructure.
V2 uses a frozen model architecture — no LoRA adapters are trained or applied.
They are preserved for reference. V1 is tagged as v1.0.0-alpha if you need
the original context.
