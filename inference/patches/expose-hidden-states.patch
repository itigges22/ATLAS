diff --git a/tools/server/server-context.cpp b/tools/server/server-context.cpp
index 4d162f8..fc26f20 100644
--- a/tools/server/server-context.cpp
+++ b/tools/server/server-context.cpp
@@ -18,11 +18,16 @@
 #include <algorithm>
 #include <cstddef>
 #include <cinttypes>
+#include <cstring>
+#include <cstdlib>
 #include <exception>
 #include <memory>
 #include <filesystem>
+#include <set>
 #include <utility>
 
+#include "ggml-backend.h"
+
 // fix problem with std::min and std::max
 #if defined(_WIN32)
 #define WIN32_LEAN_AND_MEAN
@@ -36,6 +41,57 @@ using json = nlohmann::ordered_json;
 
 constexpr int HTTP_POLLING_SECONDS = 1;
 
+// ATLAS PC-202: per-layer residual stream capture for hidden-states extension.
+// Filled by update_slots() before llama_decode (from the union of hidden_states_layers
+// across active embedding slots), populated by atlas_capture_cb_eval during decode,
+// then consumed by send_embedding which slices per-slot tokens out of the captured
+// flat buffers. Cleared at the end of each update_slots iteration.
+struct atlas_hidden_states_capture {
+    std::set<int>                       layers_wanted;   // union across active slots
+    std::map<int, std::vector<float>>   captured;        // layer -> flat [hidden_dim * batch_n_tokens]
+    int32_t                             hidden_dim = 0;
+    int32_t                             batch_n_tokens = 0;
+
+    void clear() {
+        layers_wanted.clear();
+        captured.clear();
+        hidden_dim = 0;
+        batch_n_tokens = 0;
+    }
+};
+
+// cb_eval callback installed at server init. Called for each ggml node during decode.
+// Two-phase: ask=true asks whether to materialize data, ask=false delivers it.
+// Always returns true (returning false would abort graph compute).
+static bool atlas_capture_cb_eval(struct ggml_tensor * t, bool ask, void * user_data) {
+    auto * cap = static_cast<atlas_hidden_states_capture *>(user_data);
+    if (cap == nullptr || cap->layers_wanted.empty()) {
+        return true;  // not capturing this batch — let compute proceed
+    }
+    // Match "l_out-{N}" (post-block residual; verified against Qwen3.5-9B GGUF
+    // — both attention and DeltaNet blocks produce this name).
+    if (std::strncmp(t->name, "l_out-", 6) != 0) {
+        return true;
+    }
+    const int layer_idx = std::atoi(t->name + 6);
+    if (cap->layers_wanted.find(layer_idx) == cap->layers_wanted.end()) {
+        return true;
+    }
+    if (ask) {
+        return true;  // yes, materialize this tensor's data
+    }
+    // ask=false: data is materialized — copy it out (tensor may be on GPU,
+    // ggml_backend_tensor_get handles the device->host copy).
+    if (t->type != GGML_TYPE_F32) {
+        return true;  // unexpected dtype; skip rather than corrupt the buffer
+    }
+    const size_t n = ggml_nelements(t);
+    auto & buf = cap->captured[layer_idx];
+    buf.resize(n);
+    ggml_backend_tensor_get(t, buf.data(), 0, n * sizeof(float));
+    return true;
+}
+
 // state diagram: https://github.com/ggml-org/llama.cpp/pull/9283
 enum slot_state {
     SLOT_STATE_IDLE,
@@ -689,6 +745,10 @@ private:
 
     bool sleeping = false;
 
+    // ATLAS PC-202: hidden-states capture state (see top of file for struct def).
+    // Lives for the server's lifetime; cb_eval points at it via user_data.
+    atlas_hidden_states_capture hidden_states_capture;
+
     void destroy() {
         llama_init.reset();
 
@@ -735,6 +795,12 @@ private:
 
         params_base = params;
 
+        // ATLAS PC-202: install hidden-states capture callback BEFORE context init.
+        // The callback always runs (cheap when no layers are requested) and consults
+        // hidden_states_capture.layers_wanted to decide whether to copy out data.
+        params_base.cb_eval           = atlas_capture_cb_eval;
+        params_base.cb_eval_user_data = &hidden_states_capture;
+
         llama_init = common_init_from_params(params_base);
 
         model_tgt = llama_init->model();
@@ -1739,6 +1805,25 @@ private:
             res->embedding.emplace_back(embd, embd + n_embd_out);
         }
 
+        // ATLAS PC-202: slice per-token hidden states for this slot
+        if (!slot.task->params.hidden_states_layers.empty()
+                && hidden_states_capture.hidden_dim > 0) {
+            const int H = hidden_states_capture.hidden_dim;
+            res->hidden_dim = H;
+            for (int layer_idx : slot.task->params.hidden_states_layers) {
+                auto it = hidden_states_capture.captured.find(layer_idx);
+                if (it == hidden_states_capture.captured.end()) continue;
+                const auto & flat = it->second;
+                auto & out_layer = res->hidden_states[layer_idx];
+                for (int j = 0; j < batch.n_tokens; ++j) {
+                    if (batch.seq_id[j][0] != slot.id) continue;
+                    const size_t off = (size_t)j * (size_t)H;
+                    if (off + (size_t)H > flat.size()) break;
+                    out_layer.emplace_back(flat.begin() + off, flat.begin() + off + H);
+                }
+            }
+        }
+
         SLT_DBG(slot, "%s", "sending embeddings\n");
 
         queue_results.send(std::move(res));
@@ -2951,6 +3036,22 @@ private:
                 batch.logits   + i,
             };
 
+            // ATLAS PC-202: populate hidden-states capture state for THIS decode pass.
+            hidden_states_capture.clear();
+            for (auto & slot : slots) {
+                if (slot.is_processing()
+                        && slot.task->type == SERVER_TASK_TYPE_EMBEDDING
+                        && !slot.task->params.hidden_states_layers.empty()) {
+                    for (int l : slot.task->params.hidden_states_layers) {
+                        hidden_states_capture.layers_wanted.insert(l);
+                    }
+                }
+            }
+            if (!hidden_states_capture.layers_wanted.empty()) {
+                hidden_states_capture.batch_n_tokens = n_tokens;
+                hidden_states_capture.hidden_dim     = llama_model_n_embd(model_tgt);
+            }
+
             const int ret = llama_decode(ctx_tgt, batch_view);
 
             metrics.on_decoded(slots);
@@ -4535,6 +4636,49 @@ std::unique_ptr<server_res_generator> server_routes::handle_embeddings_impl(cons
         }
     }
 
+
+    // ATLAS PC-202: optional per-layer residual capture
+    std::vector<int> hidden_states_layers;
+    if (body.count("layers") != 0) {
+        if (res_type == TASK_RESPONSE_TYPE_OAI_EMBD) {
+            res->error(format_error_response(
+                "\"layers\" is not supported on the OAI-compat /v1/embeddings endpoint; "
+                "use /embedding or /embeddings instead", ERROR_TYPE_INVALID_REQUEST));
+            return res;
+        }
+        const json & layers_json = body.at("layers");
+        if (!layers_json.is_array()) {
+            res->error(format_error_response(
+                "\"layers\" must be an array of non-negative integers", ERROR_TYPE_INVALID_REQUEST));
+            return res;
+        }
+        if (layers_json.size() > 8) {
+            res->error(format_error_response(
+                "\"layers\" must contain at most 8 entries (memory bound)",
+                ERROR_TYPE_INVALID_REQUEST));
+            return res;
+        }
+        const int n_layer = llama_model_n_layer(ctx_server.model_tgt);
+        std::set<int> seen;
+        for (const auto & v : layers_json) {
+            if (!v.is_number_integer()) {
+                res->error(format_error_response(
+                    "\"layers\" entries must be integers", ERROR_TYPE_INVALID_REQUEST));
+                return res;
+            }
+            const int l = v.get<int>();
+            if (l < 0 || l >= n_layer) {
+                res->error(format_error_response(
+                    "\"layers\" entries must be in [0, n_layer); n_layer = "
+                        + std::to_string(n_layer), ERROR_TYPE_INVALID_REQUEST));
+                return res;
+            }
+            if (seen.insert(l).second) {
+                hidden_states_layers.push_back(l);
+            }
+        }
+    }
+
     // create and queue the task
     json responses = json::array();
     auto & rd = res->rd;
@@ -4550,6 +4694,9 @@ std::unique_ptr<server_res_generator> server_routes::handle_embeddings_impl(cons
             task.params.res_type = res_type;
             task.params.embd_normalize = embd_normalize;
 
+            // ATLAS PC-202
+            task.params.hidden_states_layers = hidden_states_layers;
+
             tasks.push_back(std::move(task));
         }
         rd.post_tasks(std::move(tasks));
diff --git a/tools/server/server-task.h b/tools/server/server-task.h
index 64bdecd..f0d4721 100644
--- a/tools/server/server-task.h
+++ b/tools/server/server-task.h
@@ -87,6 +87,9 @@ struct task_params {
     // Embeddings
     int32_t embd_normalize = 2; // (-1=none, 0=max absolute int16, 1=taxicab, 2=Euclidean/L2, >2=p-norm)
 
+    // ATLAS PC-202: per-layer residual stream capture (post-block, "l_out-{N}")
+    std::vector<int> hidden_states_layers;
+
     json format_logit_bias(const std::vector<llama_logit_bias> & logit_bias) const;
     json to_json(bool only_metrics = false) const;
 };
@@ -470,6 +473,10 @@ struct server_task_result_embd : server_task_result {
 
     int32_t n_tokens;
 
+    // ATLAS PC-202: per-layer residual hidden states
+    std::map<int, std::vector<std::vector<float>>> hidden_states;
+    int32_t hidden_dim = 0;
+
     // response formatting
     task_response_type res_type = TASK_RESPONSE_TYPE_NONE;
 
